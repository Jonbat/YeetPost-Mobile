// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

import 'package:flutter_test/flutter_test.dart';
import 'package:yeetpost/yeet.dart';

int upvoteCount = 3;
int flagCount = 2;
/*
void main() {
  testWidgets('Location renders correctly', (WidgetTester tester) async {
    await tester.pumpWidget(
      Yeet("Yeet Text", false, upvoteCount, flagCount)
    );

    // Verify that our counter has incremented.
    expect(find.text ('Yeet Text'), findsOneWidget);
    expect(find.text ('upvoteCount'), findsOneWidget);
    expect(find.text ('flagCount'), findsOneWidget);
  });

}
*/
